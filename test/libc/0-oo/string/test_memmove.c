#include <string.h>
#include <stdio.h>

int main(void) {

    // TODO compiler doesn't have support for this yet
    /*
    // move down
    {
        char bytes[] = {"Hello world"};
        memmove(bytes + 5, bytes + 8, 4);
        if (0 != strcmp(bytes, "Hellorld")) {
            return 1;
        }
    }

    // move up
    {
        char bytes[] = {"Hello world"};
        memmove(bytes + 3, bytes, 4);
        if (0 != strcmp(bytes + 3, "Hellorld")) {
            return 1;
        }
    }
    */

    return 0;
}
